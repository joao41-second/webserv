Headers

Os Headers sao cirados pelo navegador e pelo .py podendo ser alterados pelo webserver se nessesario.

nao deve ser nessesario implementar.


Os Headers segem o padram:

val-fo-heder: value

Header HTTP enviado	Variável de ambiente CGI
X-Usuario: joao123	HTTP_X_USUARIO=joao123
X-App-Token: abc987	HTTP_X_APP_TOKEN=abc987
X-Custom-Flag: true	HTTP_X_CUSTOM_FLAG=true


regras para passar de Headers para env:

- passa _

e addicionado HTTP_ antes do nome do resto da variavel 

Tambem exite a possibidiade e exitir variaveis na url se sim elas sao postas em uma env


BODY: 

o copor da mensagem que  e separado dos Headers por uma linha vazia 
vai ser direcionado apara o STDIN e sera cirada uma env CONTENT_LENGTH para incicar o tamanho
