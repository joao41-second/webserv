

int socketpair(int domain, int type, int protocol, int sv[2]);

Premite ligasao de dois sockets dentro do mesmo host.

---
int socket(int domain, int type, int protocol);

Premite ligar dois sockets fora do local host e dentro tambem 
---

var Domanin

AF_INET → IPv4

AF_INET6 → IPv6

AF_UNIX → Comunicação local (entre processos na mesma máquina)

var type

SOCK_STREAM → TCP (conexão confiável e orientada a fluxo)

SOCK_DGRAM → UDP (não confiável, sem conexão)

SOCK_RAW → Acesso direto a protocolos de rede

var protocol 
	normalmente e 0 for padram mas ha vairos tipo tcp 

